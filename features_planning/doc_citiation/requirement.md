# Task: Add citation and bounding box for document retrieval

## Context
1. When select the document, the document content is showing up along with chat UI.
2. the citation now is showing as text only
3. the ocr service able to return the bounding box in the result.

## Requirements
### 1. check the current system architecture for ingestion and retrieval
### 2. provide the plan to add support for the citation by bounding box. Flow is:
- in the answer part, there is a button with "citation" icon
- upon user click, the bounding box related to the anwser will be draw and show to the user.

## Reference Files
1. D:\development\chatbot\wiki: wiki of system architecture 